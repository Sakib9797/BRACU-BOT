# Copy this file to `.env` and fill in real values. `.env` is git-ignored.
#
# Used by `data collection/scrape_via_api.py`.
# Get a free key (1000 requests/month) at https://www.scraperapi.com/
SCRAPERAPI_KEY=your-key-here